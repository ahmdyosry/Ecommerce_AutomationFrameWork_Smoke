<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">

<suite name="smokesuite" parallel="tests" thread-count="4">
    <listeners>
        <listener class-name="listeners.TestListener"/>
    </listeners>

    <test name="smoke tests" parallel="methods" thread-count="2">
        <parameter name="browser" value="chrome"/>
        <groups>
            <run>
                <include name="smoke"/>
                <exclude name="smokeLoggedIn"/>
            </run>
        </groups>

        <packages>
            <package name="tests.*"/>
        </packages>
    </test>

    <test name="loggedIn smoke tests" parallel="none">
        <parameter name="browser" value="chrome"/>
        <groups>
            <run>
                <include name="smokeLoggedIn"/>
            </run>
        </groups>

        <packages>
            <package name="tests.*"/>
        </packages>

    </test>

</suite>